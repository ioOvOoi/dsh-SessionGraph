# CONTEXT — dsh-SessionGraph

## 术语

- **对话点 / 节点 (Node)**:会话图谱的基本单元——一条消息(用户或助手)或一次 agent 切换(子 agent 启动/结束)。图谱上实时生长的最小单位。
- **分支 (Branch)**:节点间的父子关系构成的路径。从同一父节点长出的多条路径即分支。分支是会话的天然结构,不是人为创建的操作。
- **检查点 / 游标 (Cursor)**:会话当前所在的分支末端(active leaf)。会话任何时候只有一个游标,游标所在的路径即**当前路径**。
- **跳转 (Jump)**:把游标从当前位置移到某个历史节点,之后的新消息从该节点长出——同一会话内换枝,不是另开会话(区别于 Pi 的 /fork)。
- **SessionGraph**:本插件产品名。一张会话图谱 + 跳转能力 + 远期:上下文压缩、子 agent 全局图谱、图谱即主界面。
- **上下文压缩 (Compaction)**:远景能力——把被跳过的分支/历史节点压成摘要,换取无限上下文。地图明确不进第一版。

## 已定决策

- 目的地:DSH 动态插件原型,实时会话图谱,验收走通完整演示路径。
- 图粒度:消息为节点,agent 切换为特殊节点。
- 布局:横向时间线 + 分支,当前路径高亮。
- 交互:Pi /tree 式同会话跳转(游标移动),非 fork。
- 节点详情:全文 + 元数据。
- 界面:原型 = 叠加面板;究极版 = 图谱即主界面(远景)。
- 目标:演示/分享级视觉效果。
