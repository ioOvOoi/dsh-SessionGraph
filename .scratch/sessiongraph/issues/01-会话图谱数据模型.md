# 01-会话图谱数据模型

Type: grilling
Status: open
Blocked by:

## Question

会话图谱的节点/边/游标如何建模与持久化?

**已确认的平台事实**(研究:`docs/dsh-host-event-system-research.md`):
- 消息实时流唯一入口 = `session/event`(session, event),event.type ∈ {user/message, assistant/message, tool/result, assistant/chunk, turn/start|end, step/start|end, ...}
- DSH 会话 = 线性 append-only 日志,无会话内分支概念;原生分支 = fork 新会话;跨会话追溯 = `traceSession`
- 用户已定:Pi /tree 式同会话换枝(非 fork)→ 候选方案 A:插件维护 sidecar 会话图(nodes + parentId + activeCursor)

**要决策的**:
1. 节点模型:哪些事件成节点?(user/message、assistant/message、tool/result 必成节点;turn/step 边界、agent 切换标记是否成节点?)
2. sidecar 图的数据结构(节点字段、parentId、游标)与持久化位置
3. 历史重建:插件冷启动时如何从会话日志重放构建图(增量 vs 全量)
4. 游标(activeCursor)的表示、存储与多会话隔离

## Answer

(待决议)
