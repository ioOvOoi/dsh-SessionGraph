# 03-实时事件接入

Type: research
Status: open
Blocked by:

## Question

实时订阅哪些 Host 事件、如何构建消息/agent 切换节点流?

**已知方向**(研究产出先行,待闭合):
- 核心:监听 `session/event`(session, event),按 event.type 判别建节点
- 生命周期:`session/created`/`session/disposed`(建/删会话顶点)、`agent/status`(运行态)、`subagent/start|end`(父子边)
- 反查:`ctx.sessionQuery.traceSession`/`listEvents`/`readSession` 补全历史
- 关联:`session.id === agent.id`

**要确认的**(研究完成后闭合):
- 事件参数结构与 scope 过滤的精确用法
- 子 agent 消息是否也走各自会话的 session/event(是:每个子 agent 有自己的会话)
- agent 切换"特殊节点"的事件依据(agent/created?subagent/start?)

## Answer

(研究产出已基本覆盖,见 `docs/dsh-host-event-system-research.md`;待补充存储/压缩部分后闭合)
