# 05-面板与交互设计

Type: prototype
Status: open
Blocked by: 04

## Question

conversation.view tab 接入、节点详情、跳转交互、主题跟随的具体设计?

**已确认的平台事实**:
- **最佳挂载点**:`conversation.view`(list, scope: session)——会话内视图 tab 环;官方 `ui-trajectory` 的注册范式现成:`ctx.slots.inject("conversation.view", () => ctx.slots.register({ name, id, order, label, inject }, Component))`;组件自动获得 sessionId + useSession kit
- 备选:shell.overlay(帧级浮层)、details(右列面板)
- 数据通道:useProjection(key, selector);主题:theme tokens(Client Theme Inspect)
- 演示/分享目标:视觉打磨优先级高

**要决策的**:
1. 面板形态:conversation.view 新增 graph tab(推荐,与 chat/trajectory 平级)vs header 按钮 + shell.overlay 浮层
2. 节点详情:点击节点 → 详情呈现形式(内嵌面板/弹出),内容 = 全文 + 元数据(时间、角色、agent、seq、token)
3. 跳转交互:点击节点 → 确认 → 切 activeCursor(配合 T02 决策);当前路径高亮
4. 主题跟随:theme tokens 的用法(明暗自适应)
5. 空状态/加载状态/演示路径的 UI 打磨

**产出**:prototype 原型链接为资产

## Answer

(待决议)
