# 04-图谱渲染引擎

Type: prototype
Status: open
Blocked by: 01

## Question

横向时间线 + 分支的图谱如何渲染?节点生长动画怎么做?(演示/分享目标)

**已确认的平台事实**:
- **全 DSH 树无任何图布局/绘图库**(dagre/cytoscape/reactflow/d3 全部 0 匹配)→ 布局与渲染需自造
- 建议:纯 SVG/DOM + 复用 `dsh-client-ui-primitives` 组件栈;Client 组件是 React(React.createElement)
- 参考:`ui-trajectory` 的 TrajectoryTimeline(单会话时间线,已注册 conversation.view tab);`ui-subagent` 的递归子代理树(role="tree")
- 实时数据:useProjection(key, selector) 消费 Host 投影

**要决策的**:
1. 渲染技术:纯 SVG 自绘(推荐,无依赖)vs Canvas(大图性能)vs 引入布局库(增加依赖)
2. 布局算法:时间横向(X 轴)、分支上下展开(Y 轴)、当前路径高亮、按 agent 分组折叠、被遮蔽段(压缩点)显示
3. 节点生长动画(新节点出现的过渡;演示冲击力)
4. 长会话性能:节点量级、DOM 数量、虚拟化策略

**产出**:prototype 原型(布局方案 + 可交互 demo),链接为资产

## Answer

(待决议)
